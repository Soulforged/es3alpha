defmodule Es3Alpha.StorageTest do
  @moduledoc false

  alias Es3Alpha.Storage

  test "file smaller than chunk size is persisted in a single node" do
  end

  test "raises on unknown entries" do
  end
end
